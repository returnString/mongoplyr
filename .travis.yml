language: R
r:
  - "3.3"
sudo: false
cache: packages
warnings_are_errors: false

r_packages:
  - covr

services:
  - mongodb

before_script:
  - bash tests/download_datasets

after_success:
  - Rscript -e 'covr::coveralls()'